export const allProducts = (state) => {
    return state.products;
}
export const single_product = (state) => {
    return state.single_product;
}

export const product_images = (state) => {
    return state.images;
}

export const product_variants = (state) => {
    return state.variants;
}
